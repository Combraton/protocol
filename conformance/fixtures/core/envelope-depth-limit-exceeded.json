{
  "format": "combraton-conformance-fixture/1",
  "id": "core.envelope.depth-limit-exceeded",
  "version": 1,
  "title": "Nesting deeper than the declared limit is refused with limit_exceeded",
  "requirements": [
    "CORE-5"
  ],
  "spec_refs": [
    "docs/spec/profiles/CORE.md#9-limits"
  ],
  "role": "provider",
  "profiles": [
    {
      "name": "core-test",
      "major": 1
    }
  ],
  "polarity": "negative",
  "kills": [
    "ignore-limits"
  ],
  "config": {
    "limits": {
      "max_depth": 6
    }
  },
  "steps": [
    {
      "step": "start"
    },
    {
      "step": "negotiate"
    },
    {
      "step": "command",
      "operation": "core-test.subject.put",
      "command_id": "cmd-1",
      "subject": {
        "kind": "core-test.subject",
        "id": "s-1"
      },
      "preconditions": [
        {
          "subject": {
            "kind": "core-test.subject",
            "id": "s-1"
          },
          "revision": 0
        }
      ],
      "authority_epoch": 0,
      "payload": {
        "value": "v"
      },
      "extensions": {
        "example.org/deep": [
          [
            [
              [
                [
                  [
                    [
                      [
                        "deep"
                      ]
                    ]
                  ]
                ]
              ]
            ]
          ]
        ]
      },
      "expect": {
        "error": "limit_exceeded",
        "details": {
          "limit": "max_depth"
        }
      }
    },
    {
      "step": "query",
      "operation": "core-test.subject.applied_count",
      "payload": {
        "subject": {
          "kind": "core-test.subject",
          "id": "s-1"
        }
      },
      "expect": {
        "ok": {
          "applied_count": 0
        }
      }
    },
    {
      "step": "stop"
    }
  ]
}
