# M4 task — Evidence and Context profiles

- **Task:** milestone M4 of [Protocol 0.1](PLAN.md), tracked in [issue #1](https://github.com/Combraton/protocol/issues/1).
- **Owner:** Protocol session (Claude Code). **Reviewer:** repository owner.
- **Start:** the owner conditionally accepted M3 on 2026-09-14. PR #4 merged as `481cf7f`, and M4 began on branch `release-0.1/m4` from that commit, in a separate draft PR.
- **Status:** steps 1–8 done. M4 is presented for owner acceptance and not merged; see [Status](#status) and [Points for the owner](#points-for-the-owner-at-acceptance).
- **Sources:** protocol `docs/spec/SPEC.md` §2, §8–§10, §12–§14; cbr `docs/spec/SPEC.md` and `docs/spec/PREPARATION-AND-DELIVERY.md` at `3278393`; pio `docs/spec/STANDALONE-CLIENT.md` at `e65b7c0`.
- **Matrix rows:**
  - EVD-1 to EVD-8;
  - CTX-1 to CTX-20;
  - EXE-21 (cross-profile with Execution, kept explicit);
  - SCN-1, SCN-4, SCN-5, SCN-6, SCN-8, SCN-9 and SCN-11.

## Outcome

Two profiles any provider can implement, and callers and executors can rely on:
- `evidence/1`: exact artifact identity, staged upload and seal, provenance and coverage, authorized query and fetch, manifests, availability and retention with proof-loss reports, and work bindings;
- `context/1`: requests with basis, obligations, reliance and limits; shared jobs with separate subscriber cancellation; exact packets with citations, labels, coverage and gaps; updates, corrections and stale-basis detection; and behavior when the provider is unavailable.

Conformance must distinguish honest from optimistic providers with **reference participants only**: a reference evidence provider with a scripted store, a reference context provider with scripted preparation, and the M3 reference executor. **M4 does not implement the real CBR memory engine or PIO adapters**; their evidence belongs to their own gates.

## Contract drafts

- [EVIDENCE](../../spec/profiles/EVIDENCE.md): identities, descriptor, upload and seal, inspect/query/fetch, manifests, rights, availability and retention, work bindings, execution outcome references (EXE-21), events.
- [CONTEXT](../../spec/profiles/CONTEXT.md): identities, requests, jobs and cancellation, results and packets, inspect and expand, direct fetch, updates, corrections and staleness, provider unavailability, rights, events.
- [MATRIX](MATRIX.md): every M4 row now states concrete positive, adversarial, fault, cross-profile and mutant evidence.

## Scope

| Area | M4 delivers | Rows |
|---|---|---|
| Exact identity | Evidence references `{ provider?, artifact, digest }`; packet references `{ packet, revision, artifact: { provider, artifact, digest } }`; locators never identity | EVD-1, CTX-5 |
| Upload and seal | Bounded chunks, verified seal, idempotence, partial and mismatched uploads refused | EVD-2 |
| Provenance and coverage | Producer bound to the session principal, capture uncertainty, anchors, required coverage | EVD-4, EVD-7 |
| Authorized access | Query and fetch without existence leaks; expand only newly authorized content; packet-bound read grants | EVD-5, CTX-11, CTX-16 |
| Availability and retention | Availability states, holds, purge with proof-loss report, integrity on read | EVD-6 |
| Manifests | Required children and completeness | EVD-3 |
| Work bindings and outcome references | Destination and producer constraints; execution outputs as sealed evidence | EVD-8, EXE-21 |
| Requests and results | Basis, items with obligations and reliance, three separate limits, labels, per-producer coverage, `budget_insufficient`, explicit refusal of unsupported semantics | CTX-1 to CTX-9, CTX-14, CTX-20 |
| Missing required context | Unmet at deadline, never downgraded; advisory fallback | CTX-7, SCN-4 |
| Corrections and stale basis | Correction during preparation; revalidation at admission | CTX-12, CTX-13, CTX-15, CTX-17, SCN-5, SCN-9 |
| Shared jobs | Subscriber cancellation separate from the job | CTX-10, SCN-11 |
| Provider outage | Advisory proceeds with a gap; required waits | SCN-8 |
| Preparation/resource cycle | No reservation while waiting; job-initiated executions carry origin and are not re-enriched | CTX-18, CTX-19, SCN-6 |
| Direct composition | Caller, context provider, executor and evidence provider end to end | SCN-1 |
| Independent evidence | A spec-only helper extends the independent implementation to the claimed Evidence and Context features | REL-6 |
| CI evidence | Manifests, transcripts and mutant summaries with distinct outcomes | REL-8 |

Knowledge claims inside packets (claim identity, reliance decisions, applicability evaluation) are M5. M4 packets carry labels, not claim revisions.

## Owner decisions (2026-09-15)

| # | Decision | Where |
|---|---|---|
| M4-Q1 | Bounded in-band chunks and ranged fetch for 0.1, with base64 and envelope overhead in the frame limit; interrupted-upload recovery and duplicate or conflicting chunks defined explicitly; locators informational with no implicit credential-bearing fetch; out-of-band transfer deferred | EVIDENCE §3, §4 |
| M4-Q2 | Each packet revision maps immutably to one exact sealed Evidence artifact; Evidence identity, integrity, retention and fetch are reused; a protocol dependency, not another running service; excerpts and omission markers never carry the artifact's digest | CONTEXT §1, §2, §5, §6 |
| M4-Q3 | Separate reference participants over public protocol connections with isolated state; a shared executable is acceptable; one grant per audience, bound to the reader and exact object; a reference grants nothing | CONTEXT §7, §12; EVIDENCE §8 |
| M4-Q4 | The executor checks typed, observable conditions only; `match`, `mismatch` and `unavailable` are distinct, and unavailable is unknown; effect by obligation; revalidation at every relevant boundary; checks recorded without rewriting packets; a negotiated, versioned Execution extension with compatibility fixtures | EXECUTION §13.1; CONTEXT §8; CMP-8 |
| M4-Q5 | New Evidence error codes, with `artifact_digest_mismatch` naming a reference mismatch; each code's ordering, details, state change and retry defined; `upload_incomplete` is `after_reconcile`, not `same_command`, with a partial-upload → append → seal fixture | EVIDENCE §6; CORE §12 |
| M4-Q6 | Purge refused while active holds remain; naming holds is intent only; purge authority plus release authority for every named hold; hold ownership and delegated release; hold revisions checked; consistent commit; tombstones, durable proof-loss records, requested versus confirmed deletion; loss reports respect reader authorization and declare coverage | EVIDENCE §9 |
| M4-Q7 | Versioned Protocol manifest format now, in-toto export later; children name exact artifact and digest; completeness per reader authorization, with unverified distinct from known missing | EVIDENCE §7 |

**Small additions made while incorporating these:**
- **Error codes:** `upload_offset_mismatch` and `upload_size_exceeded`, needed for the explicit chunk rules M4-Q1 asks for.
- **Operation:** `evidence.upload.abandon`, because received bytes are never overwritten, so a digest-mismatched upload can only be abandoned.

**Clarifications made while implementing step 2** (applications of the decisions above, recorded in EVIDENCE):
- **Query `filtered`** is `true` whenever the reader's authorization does not cover every artifact, not only when an unreadable artifact matched. Otherwise a digest filter would be an existence test, which M4-Q5 forbids.
- **Operations without their target state** (append or abandon without a staged upload, seal of an abandoned artifact, fetch of an unsealed one) are `not_found` to an authorized caller, following Execution's precedent for answered actions; `evidence.inspect` still reports the artifact.
- **Repeated purge** of an artifact already `purge_pending` or `purged` returns its availability at the unchanged revision, like a repeated seal.
- **Not available:** fetch returns the availability and no bytes; inspect and query report availability as observed at the read, including integrity failures, without recording an event.
- **Chunk limit** is bounded by the string, payload and frame limits together.
- **Producer principal:** a descriptor may repeat the session principal; naming another is refused.

## Work plan

Each step is committed and pushed with its verification; CI uploads its results. The order puts single-provider work before the multi-participant runner.

1. **Contracts and acceptance matrix** — drafted, then updated with owner decisions M4-Q1 to M4-Q7.
2. **Evidence reference provider:** schemas, operations and scripted store, with fixtures and mutants for EVD-1 to EVD-7. This includes the upload-retry, error-ordering, manifest and hold-authority negative cases.
3. **Context reference provider:** schemas, scripted preparation, packets published as Evidence artifacts, and fixtures and mutants for the single-provider CTX rows.
4. **Multi-participant runner and protocol client:** composition descriptors, separate reference participants with isolated stores, a reference protocol client for provider-to-provider calls, and per-audience grants.
5. **Execution integration:** `execution.context_revalidation` and `execution.evidence_outputs` with compatibility fixtures, packet fetch and digest check, origin and non-enrichment.
6. **Cross-profile scenarios:** EVD-8, EXE-21, CTX-1, CTX-10, CTX-15, CTX-16, CTX-18, CTX-19, and SCN-1, 4, 5, 6, 8, 9, 11, including the cross-provider grant negatives.
7. **Independent pass:** spec-only helper in a separate worktree; divergences resolved as in M3.
8. **Close-out:** state, handoff and PR evidence; presentation for owner acceptance before merge.

## Acceptance

None may be dropped silently. If one cannot be met, it is escalated with evidence and recorded as deferred.

1. **EXE-21 is explicit.** Execution outcomes reference sealed artifacts by artifact and exact digest; references resolve only through authorized Evidence operations; mutants for unsealed or mismatched references are killed.
2. **Exact artifact and packet identity.** References carry digests; fetched bytes equal the sealed bytes; locators are never identity; regenerated or relabeled bytes are detected.
3. **Authorized access.** No existence leaks in inspect, query, fetch or expand; packet-bound read grants cover only their packet and audience.
4. **Provenance and availability.** Producer bound to the session principal; required coverage; availability states; purge with proof-loss report; integrity failure never serves bytes.
5. **Missing required context.** Unmet at deadline, never satisfied or downgraded; advisory follows its declared fallback; bound work is not admitted as ready.
6. **Corrections during preparation.** An older derivation never becomes current for a corrected binding item.
7. **Stale basis.** Detected before admission; work bound to a stale packet is not admitted as ready.
8. **Shared-subscriber cancellation.** Cancelling one request never cancels a job other requests need.
9. **CBR outage.** Advisory proceeds with a visible gap; required waits at its named boundary; no fail-open.
10. **Preparation/resource dependency cycle.** A consumer waiting for context holds no execution reservation; job-initiated executions carry their origin and are not re-enriched.
11. **Reference participants only.** No fixture depends on the real CBR memory engine or on PIO adapters; scripted providers are test environment, never protocol.
12. **Independent evidence.** The independent implementation passes the fixtures for what it claims, or its divergences are resolved.
13. **CI.** Green, with manifests, transcripts and mutant summaries uploaded and outcomes kept distinct; coverage limits are recorded, never counted as passes.

## Status

- **2026-09-15, step 1 drafted:** contract drafts, refined matrix, this packet, questions M4-Q1 to M4-Q7.
- **2026-09-15, owner decisions M4-Q1 to M4-Q7:** incorporated into EVIDENCE, CONTEXT, EXECUTION §13.1–§13.2, CORE §12 and the matrix (new row CMP-8). Next: step 2.
- **2026-09-15, step 2, Evidence reference provider:**
  - **Contract:** `schemas/evidence/1` (10 operations), six Evidence error codes in Core error data, and the `evidence_store` launch control (`corrupt`, `unavailable`, `staging_timeout_seconds`, `deletion_delay_seconds`).
  - **Reference:** artifacts and holds are Core subjects, so revisions, preconditions, deduplication and events apply unchanged. Bytes are verified at seal and on every read.
  - **Fixtures:** 13 in `conformance/fixtures/evidence`. They cover exact bytes, the partial-upload → append → seal path, digest mismatch and idempotent seal, retransmitted and conflicting chunks, chunk sizing with encoding overhead, staging timeout, and descriptor, locator and coverage rules. They also cover existence-free access, integrity failure, per-reader manifest completeness, purge with hold authority and proof-loss filtering, delegated release, and negotiation gating.
  - **Mutants:** all 25 declared by these fixtures are killed: 23 new Evidence mutants and two existing gating mutants. The Evidence mutants include seal without verification, overwrite and duplicate chunks, chunk limit without overhead, locator as identity, existence leaks, mismatched references served, unreadable query results, corrupted bytes served, and manifest completeness faults. They also include purge bypassing holds, `release_holds` as authorization, unchecked hold revisions, purged before confirmation, unfiltered loss reports and release without authority.
  - **Independent:** the Python provider does not claim `evidence/1`, so these fixtures are `unsupported` for it until step 7.
  - **CI:** green on Ubuntu and macOS at `2dc3d71`; the full stdio mutant check also passed locally.
- **2026-09-15, step 3, Context reference provider:**
  - **Contract:** CONTEXT now names its operations and payload shapes: request, basis, items and checks, limits, item results, packet facts, excerpts and expand. It also defines the packet byte format (`combraton-context-packet/1`) and the script vocabulary. `schemas/context/1` has 5 operations; the launch control is `context` and the test control `context.script`.
  - **Reference:** requests, jobs and packets are Core subjects. Each packet revision is sealed as an Evidence artifact in the provider's own store, the standalone case M4-Q2 allows.
  - **Runner:** new pattern `$sha256_base64`, so a fixture checks fetched bytes against a captured digest as a reader would.
  - **Fixtures:** 9 in `conformance/fixtures/context`, one or more each for CTX-2 to CTX-14 and CTX-20 on a single provider:
    - the packet as an exact sealed artifact with labels, citations, per-producer coverage, authority revisions and a digest-free excerpt;
    - checkable items and negotiated obligations;
    - deadline versus fallback;
    - separate limits and `budget_insufficient`;
    - shared-job cancellation;
    - authorized expand;
    - updates as new revisions;
    - correction during preparation;
    - negotiation gating.
  - **Mutants:** 19 new, each killed by the fixtures declaring it.
  - **Clarifications recorded in CONTEXT:** negotiating `context/1` does not require `evidence/1` in the same session; jobs accept subscribers only until they first publish; a cancel after preparation ended is `not_found`; later revisions go only to requests from sessions that negotiated `context.updates`.
  - **Left for steps 4–6:** CTX-1, CTX-15 to CTX-19 and the scenarios, which need separate participants and Execution integration.
- **2026-09-15, step 4, multi-participant runner and protocol client:**
  - **Runner:** `start_participant`, `stop_participant` and `kill_participant` launch named participants. Each is its own process with its own data directory, configuration and stable socket, and shares the controlled clock. `connect` takes `participant` and `principal`; `credential_principals` admits peer principals.
  - **Reference protocol client** (`conformance/reference/src/peer.rs`): authenticates with its own credential, negotiates, and sends ordinary commands and queries under grants issued by the peer. No store or private call is shared.
  - **Context provider:** configured with `context.evidence_provider`, it seals packets at that provider, and a packet revision is published only after its artifact is sealed.
  - **Fixtures:** 2 in `conformance/fixtures/composition` (Unix-socket binding):
    - packets sealed at a separate evidence provider, with isolated stores, the context provider's principal as producer, no packet reported while the evidence provider is down, and publication completing after its restart;
    - direct fetch with one grant per audience, where a grant from the other provider, a grant for another object, one held by another principal, and a bare reference are each refused.
  - **Mutants:** 3 new (`packet-reported-before-seal`, `foreign-grant-honored`, `packet-grant-covers-all-packets`), each killed.
  - **Limits:** every named participant runs the descriptor under test; mixed implementations in one composition are not exercised. Compositions need the Unix-socket binding, so stdio participants skip them.
- **2026-09-15, step 5, Execution integration:**
  - **Contract:** EXECUTION §13.1 now gives binding `fetch` and `request`, separate `revalidation` and M3 `state`, and the queue reasons. It adds `context.blocked` at dispatch and transitions, check records written when a result changes, and the M3-shaped presentation for sessions without the feature (CMP-8). §13.2 lists outputs only after sealing, and submits may carry `origin`. EVIDENCE §10 binds a publish grant to work through a non-artifact resource.
  - **Schemas:** execution common and inspect gain the packet-reference binding form, conditions, fetch, request, checks, blocked, completion outputs and origin; the launch configuration gains executor `observed_basis`, `peers`, `evidence_outputs` and script step `observe_basis`.
  - **Reference executor** (`conformance/reference/src/revalidation.rs`):
    - checks typed conditions against its observed basis; an authority revision is never observable;
    - fetches packet facts and bytes through public connections under the binding's grants, verifying the reference and the digest;
    - revalidates at admission, after queueing, at dispatch and at named transitions;
    - seals completion outputs at its evidence provider under a work-bound grant.
  - **Fixtures:** 5:
    - negotiated extension and M3 presentation;
    - match, mismatch and unavailable by obligation, including a blocked transition and the request link;
    - revalidation at dispatch and after queueing across a basis change;
    - a publish grant bound to work;
    - a composition in which the executor fetches the bound packet, refuses the wrong packet revision, and lists only sealed, work-bound outputs.
  - **Mutants:** 10 new, each killed.
  - **Not tested:** a digest check on fetched bytes cannot be distinguished against honest reference providers, because they never serve mismatched bytes. The check is implemented but has no mutant.
- **2026-09-15, step 6, cross-profile scenarios:**
  - **Contract:**
    - EXECUTION §13.1: with a context fetch grant, each check re-reads the bound revision's facts. The implicit condition `packet.current` is `match` while current, `mismatch` once superseded, and `unavailable` when the context provider cannot be read. A differing reference, or facts reporting a required item unmet, mean the packet is not held.
    - CONTEXT §4: no call cycles; a context provider keeps answering reads while its investigations run.
  - **Reference:**
    - the context provider's script step `execute` runs a job's investigation at a separate executor, with `origin` and without context, followed from a worker outside the processing lock;
    - work queued for context holds no capacity slot.
  - **Runner:** queries may carry `eventually_ms`, and named participants' socket paths and credentials are known before any participant starts.
  - **Fixtures:** 5 compositions:
    - missing required context (SCN-4, CTX-7);
    - correction before dispatch, with late delivery (SCN-5, CTX-15, CTX-17);
    - preparation without the consumer's slot (SCN-6, CTX-18, CTX-19);
    - context provider outage (SCN-8);
    - one consumer cancelling while the other continues (SCN-11, CTX-10).
    The step 5 composition now also covers SCN-1: the caller resolves outputs under its own grant, and the reference alone is refused. SCN-9 now pairs the current revision with the other revision's artifact.
  - **Mutants:** 4 new (`context-outage-fails-open`, `unmet-packet-satisfies`, `context-queued-holds-capacity`, `job-execution-re-enriched`). Every mutant declared by a composition fixture is killed.
  - **Stability:** each new composition ran as intended 10 of 10 times.
- **2026-09-15, step 7, independent pass:**
  - **Sixth pass:** a spec-only helper extended the independent Python provider to `evidence/1`, `context/1` and `execution.context_revalidation` over stdio (branch `release-0.1/m4-independent`, merged).
    - Its first run, from the documents alone, gave 205 pass and 17 fail; after aligning with fixtures where the documents are silent, 219 pass and 3 fail.
    - The three failures were a real defect: the runner's and the reference's retry classes for the new Evidence codes.
  - **Resolution** ([M4-DIVERGENCES](M4-DIVERGENCES.md)), committed as `8500948`:
    - real defects fixed in the runner, the reference and CORE §12: retry classes, chunk-limit arithmetic, purge event order, checks deciding satisfaction, shared jobs across principals, hold expiry, and the queue-reason priority;
    - the documents now state what the fixtures expected;
    - 3 new fixtures, 18 re-versioned, 4 new mutants.
  - **Seventh pass:** the helper realigned the independent provider with the resolved documents (merged). It runs 225 pass, 0 fail, 4 unsupported and 20 skipped; its remaining open points are resolved in [M4-DIVERGENCES C.1](M4-DIVERGENCES.md#c1-seventh-pass-realignment-base-8500948).
  - **Evidence** of both passes (runs, transcripts) is kept locally under `conformance/results/independent-m4-pass-evidence/` (gitignored).
- **2026-09-15, step 8, close-out:** state, handoff, PR description and the issue checkpoint are updated, and CI evidence is recorded on [PR #5](https://github.com/Combraton/protocol/pull/5/checks). M4 is presented for acceptance.

## Acceptance criteria: evidence

| # | Criterion | Evidence |
|---|---|---|
| 1 | EXE-21 explicit | `composition.executor-fetches-bound-packet-and-seals-outputs` (outputs sealed under a work-bound grant, listed only when sealed, resolved by the caller under its own grant); mutants `output-listed-unsealed`, `output-digest-differs` |
| 2 | Exact artifact and packet identity | `composition.executor-verifies-assembled-packet-bytes` (adversarial altered bytes; mutant `fetched-digest-unchecked`, intended step and reason); `evidence.upload-seal-and-fetch-exact-bytes`, `evidence.descriptor-provenance-coverage-and-locator-rules`, `context.packet-is-an-exact-sealed-evidence-artifact`, `context.updates-are-new-revisions-and-old-bytes-stay-historical`, `composition.packets-are-sealed-at-a-separate-evidence-provider`; mutants for regenerated bytes, remapped revisions, relabeled revisions, locator identity |
| 3 | Authorized access | `evidence.access-reveals-no-existence` (including the paired query test), `evidence.manifest-completeness-respects-authorization`, `context.expand-returns-only-authorized-citations`, `composition.direct-fetch-needs-one-grant-per-audience`, `evidence.publish-grant-bound-to-work` (typed work binding) |
| 4 | Provenance and availability | `evidence.purge-requires-release-authority-for-holds`, `evidence.integrity-failure-is-never-served`, `evidence.expired-hold-stops-protecting`, `evidence.hold-release-needs-ownership-or-delegated-authority`, `evidence.purge-appends-the-artifact-events-first` |
| 5 | Missing required context | `context.deadline-leaves-required-unmet-and-advisory-follows-fallback`, `composition.missing-required-context-blocks-bound-work` (SCN-4) |
| 6 | Corrections during preparation | `context.correction-during-preparation-never-becomes-current`, `composition.correction-before-dispatch-blocks-the-superseded-revision` (SCN-5) |
| 7 | Stale basis | `execution.revalidation-reports-match-mismatch-and-unavailable-by-obligation`, `execution.revalidation-repeats-at-dispatch-and-after-queueing`, `execution.transition-block-clears-when-current`, `composition.pinned-and-current-bindings-treat-supersession-differently`, SCN-5 and SCN-9 compositions |
| 8 | Shared-subscriber cancellation | `context.shared-job-survives-one-subscriber-cancelling`, `composition.cancelling-one-consumer-leaves-the-other` (SCN-11) |
| 9 | CBR outage | `composition.context-provider-outage-advisory-proceeds-required-waits` (SCN-8); mutant `context-outage-fails-open` |
| 10 | Preparation/resource cycle | `composition.preparation-runs-without-the-consumer-slot` (SCN-6, CTX-18, CTX-19); `composition.blocked-consumer-releases-the-slot-for-preparation` (single slot: acquire, block, release, investigation, resume once); `execution.blocked-dispatch-releases-capacity-and-resumes`; `execution.released-work-ends-instead-of-resuming`; mutants `context-queued-holds-capacity`, `job-execution-re-enriched`, `blocked-dispatch-holds-capacity`, `resume-without-capacity`, `resume-ignores-cancellation` |
| 11 | Reference participants only | Scripted store, scripted preparation and scripted executor only; no CBR engine or PIO adapter is used |
| 12 | Independent evidence | Spec-only passes on a single provider; divergences resolved in [M4-DIVERGENCES](M4-DIVERGENCES.md). This is single-provider evidence and does not prove mixed-implementation composition. |
| 13 | CI | Green on Ubuntu and macOS for each pushed step; the final head's checks are linked from PR #5 |

Every M4 matrix row (EVD-1 to EVD-8, CTX-1 to CTX-20, EXE-21, CMP-8, SCN-1, 4, 5, 6, 8, 9, 11) is named by at least one fixture.

## Owner review of 2026-09-15 and the close-out pass

The owner reviewed the first presentation and did not yet accept or authorize the merge. The six close-out items and their resolutions:

| # | Owner direction | Resolution |
|---|---|---|
| 1 | Release capacity when blocked before dispatch; preserve identity; revalidate before resuming; do not release ownership or liability | EXECUTION §13.1 "Capacity while blocked before dispatch": the slot is released with `scheduling` and `execution.scheduling.changed`. Identity, delivery effect, budget reservation, workspace lease, host ownership and controller epoch are kept. Resumption ends the execution instead if it was cancelled, lost authorization or passed a deadline; otherwise it revalidates context, reacquires capacity, and dispatches once under the same delivery. Fixtures: `execution.blocked-dispatch-releases-capacity-and-resumes` (release, reuse, resume, a single attempt, waiting for capacity), `execution.released-work-ends-instead-of-resuming`, `execution.transition-block-clears-when-current`, and the single-slot scenario `composition.blocked-consumer-releases-the-slot-for-preparation`. Mutants `blocked-dispatch-holds-capacity`, `resume-without-capacity`, `resume-ignores-cancellation`. |
| 2 | Test executor verification against altered bytes with the expected digest metadata | Store control `evidence_store.serve_altered_bytes`, outside protocol operations. Fixture `composition.executor-verifies-assembled-packet-bytes` shows well-formed responses with altered bytes under the sealed digest: valid bytes are held, substituted bytes are never held or delivered, and the required binding stays unsatisfied. Mutant `fetched-digest-unchecked` fails at the substituted-bytes submit for the stated reason (`kill_expectations`). |
| 3 | Separate supersession from applicability; explicit current requirement; pinning never bypasses corrections; never substitute | Binding member `require_current`; implicit `packet.current` only under it; `packet.facts` reports corrections. CONTEXT §8 `invalidated_items` and `superseded_by` are reported at the read. Fixture `composition.pinned-and-current-bindings-treat-supersession-differently`; SCN-5 now uses an authority correction. Mutants `current-requirement-ignored`, `supersession-always-stale`, `pinned-ignores-correction`, `newer-revision-substituted`. Required items that are unmet stay unmet. |
| 4 | Approve the additional codes and abandon, fully documented; approve `filtered`; paired query test | EVIDENCE §4 table of authorization, preconditions, errors, state, events and idempotency; abandon never discards sealed or held content (steps in the seal and hold fixtures, mutant `abandon-sealed-accepted`). Paired query steps in `evidence.access-reveals-no-existence` v2: results before and after an unreadable matching artifact are identical; mutant `filtered-reveals-matches`. |
| 5 | Typed, explicit work-binding constraint | CORE §15.3 grant `constraints`; EVIDENCE §10 `evidence.work_binding` (feature of that name). Destination and work must both match, and delegation keeps the constraint. Fixture `evidence.publish-grant-bound-to-work` v2 covers correct, wrong and missing work, a destination outside scope, a combined grant with unrelated resources, an unknown kind, the feature not negotiated, and delegation. Mutant `work-constraint-inferred`. |
| 6 | Close-out evidence; independent checks; honest limits | See [Status](#status), the acceptance table and the coverage limits below. |

## Points for the owner at acceptance

Points 1–5 of the first presentation were decided in the owner review above: the additions and `filtered` were approved, and supersession, capacity while blocked and bound work were refined. What remains for acceptance is the evidence and these limits, stated plainly:

- **Composition limits:**
  - compositions run every named participant from the descriptor under test, so no mixed-implementation composition is exercised;
  - single-provider independent evidence does not prove composition across implementations;
  - compositions need the Unix-socket binding.
- **Capacity release:** a released execution waiting on capacity is resumed in execution-ID order of evaluation, not in a fair queue; fairness among released executions is unspecified.
- **Adversarial coverage** is one store control (altered bytes). Other lying-provider behaviors, such as altered facts or a forged reference in facts, are covered only by the reference-mismatch check.
- **Unchecked by fixtures:** the items listed in [M4-DIVERGENCES §D](M4-DIVERGENCES.md#d-still-unchecked-by-fixtures-coverage-limits).
