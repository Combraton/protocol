# M5 task — Knowledge and Verification profiles

- **Task:** milestone M5 of [Protocol 0.1](PLAN.md), tracked in [issue #1](https://github.com/Combraton/protocol/issues/1).
- **Owner:** Protocol session (Claude Code). **Reviewer:** repository owner.
- **Start:** on 2026-09-15 the owner accepted M4 at `86e128f` as a completed milestone, with its documented coverage limits. PR #5 merged as `ee82afb`. M5 began on branch `release-0.1/m5` from that commit, in draft [PR #6](https://github.com/Combraton/protocol/pull/6). Accepting M4 is not accepting or releasing Protocol 0.1.
- **Status:** the owner decided M5-Q1 to M5-Q9 on 2026-09-15 and authorized implementation within this scope. That is not authority to merge M5 or release Protocol 0.1. The decisions are incorporated in the contracts, examples and matrix ([Owner decisions](#owner-decisions-2026-09-15)). Implementation proceeds in the steps of the [work plan](#work-plan); see [Status](#status).
- **Sources:**
  - protocol `docs/spec/SPEC.md` §2, §5, §8–§13;
  - cbr `docs/spec/SPEC.md` §3–§7, §9–§11, `docs/spec/INTERNALS.md` §1–§3 and `docs/spec/PREPARATION-AND-DELIVERY.md` §8–§9, at `3278393`;
  - combraton `docs/architecture/MODEL.md` §3–§6 and `docs/architecture/VERIFICATION.md` §1–§7, at `9af69ce`.
- **Matrix rows:**
  - KNW-1 to KNW-10;
  - VER-1 to VER-5;
  - SCN-7 and SCN-16;
  - CMP-9.

## Outcome

Two profiles that any provider can implement and any reader can check:
- `knowledge/1`: claims with immutable revisions and declared ancestry, three semantic planes, structural conflict and drift records, applicability evaluations against a target basis, reliance decisions under one explicit authority binding per scope, and history with explicit links.
- `verification/1`: exact contracts, `evaluate_contract` job references, immutable receipts mapped to sealed artifacts, and a read-only assessment in which missing, unavailable or outdated verification is never a pass.

Two cross-profile additions: packets that carry claims, enforced at required boundaries; and a provider that reconnects with a changed capability.

Conformance again uses **reference participants only**: a reference knowledge provider, a reference verifier with a scripted evaluator, and the M4 context, evidence and execution references. **M5 does not build the real CBR memory engine or PIO adapters**, and it does not implement Coordination.

## Contract drafts

- [KNOWLEDGE](../../spec/profiles/KNOWLEDGE.md): claim revisions and digests, independent facets, declared ancestry and support classes, authority bindings and decisions, structural conflicts and drift, applicability precedence, history.
- [VERIFICATION](../../spec/profiles/VERIFICATION.md): contracts, jobs and pinned evaluators, receipts and their artifacts, scoped results, assessment, no authority.
- [CONTEXT §14](../../spec/profiles/CONTEXT.md#14-claims-in-packets-proposed-m5): claims in packets, with invalidated and unverified items.
- [EXECUTION §13.3](../../spec/profiles/EXECUTION.md#133-claim-revalidation-executionclaim_revalidation-proposed-m5): claim revalidation at the binding's boundary.
- [CORE §12](../../spec/profiles/CORE.md#12-errors): proposed codes `claims_not_comparable` and `contract_unavailable`.
- [MATRIX](MATRIX.md): KNW-1 to KNW-10, VER-1 to VER-5, SCN-7, SCN-16 and CMP-9.

## What M5 will establish, and what it will not

**Establishes**, once accepted with fixtures, mutants and independent evidence:
- Evidence, claims, decisions, evaluations, conflicts and receipts are separate records with separate identities; no claim or receipt records a decision.
- A claim revision never changes. Revisions, corrections, supersession and conflicts are later records with explicit links, and history keeps all of them.
- Reliance, applicability, subject health and evidence availability are four facets, each from its own records.
- Declared ancestry is classified deterministically. Shared, partial or unknown ancestry never produces corroboration, and nothing is called independent.
- A conflict needs a structural incompatibility. Disjoint qualifiers, basis or validity, and multi-valued predicates are not conflicts, and a potential conflict is never presented as demonstrated.
- Only the one bound authority for a scope decides reliance or resolves conflicts. It may adopt its own claim, and that adoption is recorded as such. Grants, derivation labels, producers and verifiers never decide.
- Applicability follows a fixed precedence. An observed mismatch is `invalid_for_target`, incomplete checks are never `applicable`, and `needs_check` and `unknown` are never shown as stale.
- A receipt names exact subjects for every contract role, the exact contract, evaluator, environment and a result for every property, and maps to one sealed artifact.
- An assessment states its time and basis. Mismatched, unverifiable, stale, before-observation, not evaluated and indeterminate are never satisfied, and a caller-selected time never establishes present validity.
- A packet carrying claims keeps immutable snapshots. A required binding whose claim loses its permitted use or becomes invalid is blocked at its boundary, unavailable knowledge is unknown, advisory work proceeds, and nothing is substituted.
- Work pinned to an evaluator keeps its meaning when a provider reconnects with that evaluator changed.

**Does not establish:**
- that any claim is true, that declared ancestry is honest, or that an evaluator is correct;
- semantic equivalence or incompatibility beyond structural comparison;
- memory quality, retrieval quality or context usefulness; those belong to CBR's own evaluation plans;
- project acceptance or gate evaluation; Coordination is unsupported in 0.1, and the local authority binding is the standalone mode;
- temporal reconstruction views, which are deferred (M5-Q8);
- signed receipts or offline verification, deferred with Remote trust (U2);
- mixed-implementation composition: M5 compositions still run one implementation for every participant (see [carried-forward coverage](#m4-coverage-carried-forward)).

## Intended behavior in examples

Identifiers are illustrative.

**1. Drift is not a contradiction; different targets are not conflicts (KNW-2, KNW-5).**
- **Drift.** Scope `svc`, subject `{ kind: "app.service", id: "billing" }`, predicate `queue`, `cardinality: single`. Revision A is `normative`, value `"v2"`, accepted for `binding` use. Revision B is `observed`, value `"v1"`, basis tree `t9`.
  - Opening a record on A and B gives kind **drift**.
  - A has no basis, so the dimension `basis` is uncertain, and the status is `potential`.
  - Both revisions keep their records and facets.
- **Disjoint qualifiers.** "workers = 5" with qualifiers `{ environment: "staging" }`, against "workers = 3" with `{ environment: "production" }`, is refused: `claims_not_comparable`, reason `qualifiers_disjoint`.
- **Multiple values.** "language = English" and "language = French" with `cardinality: multiple` are refused with `multiple_values_permitted`.
- **Demonstrated.** Two `observed` revisions are `demonstrated` only with:
  - equal qualifiers;
  - equal complete bases;
  - fully known, overlapping validity;
  - `single` cardinality.

**2. Declared ancestry, classified deterministically (KNW-4).**
- **Shared root.** Three summaries repeat "the auth flow is correct", each declaring complete root `{ kind: "evidence", provider: "cbr-1", artifact: "derivation.d1", digest: … }`. The class is `single_lineage`, however often it repeats.
- **Pairwise overlap.** Roots `{A,B}`, `{B,C}` and `{A,C}`, all complete, give `overlapping_lineages`: every pair shares an origin, and none is common to all.
- **Disjoint.** A complete `{A}` and a complete `{D}`, where `D` is a separately captured CI report, give `multiple_lineages`. That still does not say the sources are independent.
- **Unknown ancestry.** A wrapper artifact with `completeness: unknown` adds nothing. `{A}` plus that entry is `undetermined`, never `multiple_lineages`.
- **Equal digests.** Two roots with different references but equal digests are never disjoint.

**3. A model cannot pick a winner (KNW-5).**
- The CBR maintenance principal opens a record on two `interpretive` revisions about a retry limit, with a suggested test in `note`.
- Its `conflict.resolve` is `not_authority`. A newer revision from the same producer resolves nothing.
- The bound authority resolves with `narrow_scope`, and both revisions and the resolution stay in history.

**4. A human states and adopts a requirement; a model cannot (KNW-6).**
- The bound authority `owner` proposes "billing must use queue v2", then records `accepted_for_use` for `binding` use. The decision records `author_is_decider: true`, presented as adoption by its author, not review.
- Model producer `cbr-model` proposes a `normative` claim and labels its derivation `human`. Its own `decision.record` is `not_authority`, because it is not the bound authority, whatever the label says.

**5. Four facets at once (KNW-3).**

```json
{ "reference": { "provider": "cbr-1", "claim": "parser-handles-bom", "revision": 2, "digest": "sha256:…" },
  "reliance": { "state": "accepted_for_use", "permitted_use": "evidence", "decision": "dec-7", "author_is_decider": false },
  "applicability": [ { "evaluation": "ev-3", "target": { "repositories": [ { "id": "repo-a", "tree": "t12" } ], "completeness": "complete" }, "result": "invalid_for_target", "evaluator": { "id": "tree", "version": "1" } } ],
  "health": "not_applicable",
  "availability": { "state": "purged", "counts": { "available": 0, "unavailable": 0, "purged": 1, "unknown": 0 } } }
```

The claim was accepted at `t10`. At `t12` the tree named by its condition differs, and its supporting test log was purged. None of these facts rewrites the others.

**6. Applicability precedence (KNW-7).** A claim has condition `k1` (tree of `repo-a`), condition `k2` (`environment_digest`, which this evaluator does not support), and dependency `D@1`.
- Target tree differs for `k1`: `invalid_for_target`, even though `k2` is unsupported.
- Tree matches, target lacks `environment`: `unknown`.
- Anchors all present and matching, but `D@1` has no evaluation for this target: `needs_check`.
- Everything matches and `D@1` is `applicable` for this target: `applicable`.

In a packet, the `needs_check` and `unknown` cases are not `stale` and not historical.

**7. An extracted instruction stays a proposal (KNW-9).** A model extracts "always run migrations before tests" as a `normative` claim, which stays `proposed`. A packet can carry it only as a `hypothesis` or `inferred` section. A `binding` item it would satisfy is `unmet` with reason `not_accepted` until the bound authority accepts it for `binding` use. The earlier packet is never rewritten.

**8. Assessment states its basis and time (VER-1, VER-4).** Contract `c1` has roles `tree` and `output` and required properties `parser.unit` and `upload.journey`. The receipt reports `parser.unit: pass` and `upload.journey: indeterminate` (`browser_unavailable`).
- Now, with both roles matching: `parser.unit` satisfied, `upload.journey` not satisfied, `overall: not_satisfied`, `time_basis: provider_clock`.
- Omitting role `output` from the request: `invalid_envelope`.
- A contract reference at another artifact with an equal digest: `contract_mismatch`.
- The contract artifact purged: `contract: unverifiable` (`contract_unavailable`), no property satisfied.
- `at` set to last week, after `observed_until`: at most `satisfied` for that instant, with `present_validity: not_established`.
- `at` before `observed_until`: `before_observation`.

**9. Scoped observations (VER-2, VER-5).** A receipt reports `fail` for execution `e-5`'s output, and `e-5` keeps every axis. A fully satisfied receipt for claim `parser-handles-bom` leaves reliance `proposed`. The verifier's `decision.record` is `not_authority`, and the bound authority may accept the claim and cite the receipt.

**10. Reconnect with a changed evaluator (SCN-7).** Job `j1` pins `journey` version 1 and records `upload.journey: pass`. The verifier restarts with version 1 removed and version 2 installed; the caller reconnects and sees `core.capabilities.changed`.
- `j1` completes: `upload.journey: pass` stays, and its unfinished `upload.visual` is `indeterminate` (`evaluator_unavailable`).
- A new job pinning version 1 is `capability_unavailable`; one pinning version 2 runs.
- `j1`'s receipt names version 1.

**11. Claims in packets, enforced at the boundary (KNW-10, SCN-16).**
- Request `r` has a required `binding` item `i-req` and an advisory item `i-adv`, each `claim_included`. Packet `r@1` (`combraton-context-packet/2`) carries claim `A@1` for `i-req` and `B@1` for `i-adv`, both accepted for `binding` use and `applicable` to the basis.
- Execution `e-req` binds `r@1` `required_before_start`, and `e-adv` binds it `advisory`, both under `execution.claim_revalidation`.
- **Rejection.** The authority rejects `A@1`. `context.packet.inspect` reports:
  - `claim_changes: [ { section_id: "s-a", claim: A@1, change: "reliance_changed" } ]`;
  - `invalidated_items: [ { item_id: "i-req", claim: A@1, reason: "permitted_use_lost" } ]`.

  `e-req`'s `packet.facts` is `mismatch` (`claim invalidated: i-req`), so it is not dispatched (`context_binding_stale`), while `e-adv` proceeds.
- **Outage.** If instead the knowledge provider is unreachable, `unverified_items` names `i-req` (`knowledge_unavailable`), and `e-req` waits as `context_binding_unknown`.
- **Newer revision.** A newer revision `A@2` is reported `lineage_revised` and is never substituted.

## Reuse and compatibility

M5 reuses the accepted contracts and changes them only as listed.

| Contract | Reused as is | Change |
|---|---|---|
| Core | Command path and idempotency; grants and `constraints`; authority epochs (§8) for authority bindings; events; capability snapshots (§17) for evaluator versions; existence rules | **Additive:** codes `claims_not_comparable` and `contract_unavailable` (§12) |
| Evidence | Exact references, sealed artifacts, fetch, availability and purge | None. Contracts and receipts are new artifact formats (M5-Q5). |
| Context | Requests, items, labels, packets, conditions (reused by claim `conditions`), `invalidated_items` | **Negotiated extension** `context.claims`: packet format `/2`, check kind `claim_included`, `claim_changes`, claim entries in `invalidated_items`, and `unverified_items` (M5-Q7, CMP-9) |
| Execution | Status axes; `evaluation` as an attributed external reference; context revalidation, boundaries and blocking | **Negotiated extension** `execution.claim_revalidation` (§13.3): claim outcomes of `packet.facts`, enforced through the §13.1 machinery (M5-Q7) |

## Provider and consumer assumptions, with counterexamples

| Assumption | Counterexample the contract must survive |
|---|---|
| A reader can tell which revision a statement came from | A packet cites "latest": after revise, the same citation means different content. Hence exact revision references with recomputable digests. |
| Different targets are not contradictions | Staging and production settings, two supported languages, or trees `t1` and `t2` flagged as conflicts. Hence structural comparison with refusal reasons, and potential versus demonstrated. |
| An authority is identifiable per scope | Two standalone tools both configured to accept for `svc`. Hence one binding per scope, transfer by epoch. |
| Permission comes from authentication, not labels | A model labels its derivation `human` to accept its output. Hence the binding decides, and labels are descriptive. |
| Support can be compared | The same derivation wrapped in three artifacts with unknown ancestry. Hence declared, provider-qualified roots, and `undetermined` rather than forced classes. |
| Applicability is decided against a target | An accepted claim assumed valid on a new branch, or an unevaluated claim shown as stale. Hence explicit precedence, and `needs_check` or `unknown` kept distinct. |
| A receipt is about exact subjects and contract | A passing receipt for `t12` reused for `t13`, or a different contract with an equal digest. Hence every role compared and exact contract references. |
| An assessment is about a stated moment | A caller asks about last week and presents the answer as current. Hence `time_basis` and `present_validity`. |
| Unavailable is not a failure and not a pass | A browser outage recorded as `fail` stops unrelated work, or recorded as `pass` hides a missing journey. Hence `indeterminate`, `not_satisfied`, and no stop semantics. |
| Capability meaning is pinned | An evaluator upgraded mid-job appears in old receipts. Hence pinned evaluator versions and durable per-property results. |
| Readers verify what they read | A knowledge provider serves an altered claim record under its old digest. Hence recomputed digests, as the M4 executor checks packet bytes. |

## Owner decisions (2026-09-15)

| # | Decision | Where |
|---|---|---|
| M5-Q1 | Structured statements and three planes, with tighter comparison: a subject, predicate and scope ID match is not a contradiction. The comparison accounts for qualifiers, basis, validity overlap and predicate cardinality, and distinguishes a potential conflict from a demonstrated incompatibility, without becoming a semantic reasoning engine. | KNOWLEDGE §3, §7 |
| M5-Q2 | Declared ancestry, never claimed independence. Classification is deterministic for partial overlap and unknown ancestry (`{A,B}`, `{B,C}`, `{A,C}` included). A wrapper is never a new origin, references are exact and provider-qualified, and uncertainty is exposed. | KNOWLEDGE §5 |
| M5-Q3 | Exact scopes, explicit transfer, preserved decisions; only the bound authority decides, grants confer no authority, and there is no delegated deciding in 0.1. The blanket self-decision prohibition is replaced: the bound authority may adopt its own claim through a separate decision, recorded as author and decider being the same. A model producer without authority cannot accept, whatever its derivation label. | KNOWLEDGE §6 |
| M5-Q4 | An observed relevant mismatch establishes `invalid_for_target`, and incomplete checks never establish `applicable`, with precise precedence. `unknown` and `needs_check` are not stale or historical, including in packets. | KNOWLEDGE §8; CONTEXT §14 |
| M5-Q5 | Contracts and receipts reuse sealed Evidence artifacts and exact references, with the receipt-to-artifact mapping documented | VERIFICATION §2, §5 |
| M5-Q6 | Read-only assessment, separate from acceptance. It covers complete subject-role sets, exact contract references, unverifiable contract, environment or receipt, and an explicit time and basis. A caller-selected time is not present validity, and a time before the observation establishes nothing. | VERIFICATION §7 |
| M5-Q7 | Negotiated claims in packets with immutable snapshots and separately reported changes, plus boundary enforcement through the existing invalidation and revalidation contracts. Unavailable knowledge is unknown; advisory work proceeds; no silent substitution; supersession stays distinct from invalidity; cross-provider fixtures. | CONTEXT §14; EXECUTION §13.3 |
| M5-Q8 | Reconstruction APIs deferred. Immutable records, recorded order and time, supplied validity, and explicit supersession links are retained; unknown validity stays unknown. | KNOWLEDGE §9 |
| M5-Q9 | Pinned evaluator identity and version; durable property results kept; unfinished properties `indeterminate` when the pinned evaluator is lost; never switching versions or rewriting receipts | VERIFICATION §4 |

**Changes from the proposals:**
- **Codes and reasons:** the proposed `authority_scope_bound` code and `self_decision` reason are not used. A scope's binding is the subject whose ID is the scope, so a second binding fails its precondition, and self-adoption is recorded instead of refused.
- **Evaluator predicates** name each version (`verification.evaluator.<id>.v<version>`), so a version change is a capability change without altering Core's predicate shape.
- **Execution** gains the negotiated `execution.claim_revalidation` for boundary enforcement.

## Scope

| Area | M5 delivers | Rows |
|---|---|---|
| Claims and revisions | Propose, revise with base check, recomputable digests, planes | KNW-1, KNW-2 |
| Independent facets | Reliance, applicability, health, availability from their records | KNW-3 |
| Declared ancestry | Provider-qualified roots, deterministic classes, exposed uncertainty | KNW-4 |
| Conflicts and drift | Structural comparison, potential versus demonstrated, authority resolution | KNW-2, KNW-5 |
| Authority | One binding per scope, epochs, recorded self-adoption, no decision by grant or label | KNW-6, KNW-9 |
| Applicability | Target basis, precedence, pinned evaluator | KNW-7 |
| History | Immutable records with explicit links | KNW-8 |
| Contracts, jobs and receipts | Exact contracts, complete roles and properties, durable results, artifact mapping | VER-1, VER-3 |
| Scoped results and assessment | No stop semantics; complete matching rules; explicit time and basis; no authority | VER-2, VER-4, VER-5 |
| Packets carrying claims | Negotiated snapshots, read-time changes, boundary enforcement | KNW-10, SCN-16, CMP-9 |
| Reconnect with changed capability | Pinned evaluators across restart | SCN-7 |
| Independent evidence | A spec-only helper extends the independent implementation to Knowledge and Verification | REL-6 |

## Work plan

Each step is committed and pushed with its verification; CI uploads its results.

1. **Contracts, matrix and decisions.** Drafted, then updated with owner decisions M5-Q1 to M5-Q9.
2. **Knowledge reference provider:** `schemas/knowledge/1`, claims and digests, facets, ancestry classes, authority and decisions, conflicts, applicability, history, the `knowledge` launch control, and fixtures and mutants for KNW-1 to KNW-9.
3. **Verification reference provider:** `schemas/verification/1`, contracts, jobs with the scripted evaluator and pinned versions, receipts and artifacts, recording, assessment, and fixtures and mutants for VER-1, VER-3 and VER-4.
4. **Cross-profile:**
   - VER-2 with the reference executor;
   - VER-5 with the knowledge provider;
   - SCN-7 across a verifier restart;
   - `context.claims`, `execution.claim_revalidation`, KNW-10, SCN-16 and CMP-9 with separate knowledge, context, evidence and executor participants;
   - the adversarial altered-claim case.
5. **Independent pass:** a spec-only helper in a separate worktree extends the independent implementation; divergences are resolved as in M3 and M4.
6. **Close-out:** state, handoff and PR evidence, then presentation for owner acceptance before merge.

## Acceptance

None may be dropped silently. If one cannot be met, it is escalated with evidence and recorded as deferred.

1. **Separate records.** Evidence, claim revisions, decisions, evaluations, conflicts and receipts have separate identities. No operation on one records another implicitly.
2. **Revisions and history.** Immutable revisions with base checks and recomputable digests; later records with explicit links; unknown validity stays unknown.
3. **Independent facets.** Reliance, applicability, health and availability coexist in any combination.
4. **Declared ancestry.** The classification is deterministic, including partial overlap and unknown ancestry; no wrapper or equal digest manufactures corroboration.
5. **Structural conflicts.** Disjoint qualifiers, basis or validity, multi-valued predicates and equal values are refused with their reasons; potential is distinct from demonstrated.
6. **Authority.** Only the bound authority decides or resolves. Self-adoption is recorded, and grants, labels, producers and verifiers are refused.
7. **Applicability.** The precedence is enforced; incomplete checks never yield `applicable`; `needs_check` and `unknown` are never stale.
8. **Complete receipts.** Every role and property is listed; the receipt maps to one sealed artifact.
9. **No pass by absence or age.** The assessment's matching rules, time basis and `present_validity` behave as specified.
10. **Scoped observations.** A failing receipt changes no execution state; a passing receipt grants no authority.
11. **Packets carrying claims.** Snapshots are immutable and changes reported; required bindings are blocked at their boundary when a claim loses its use, becomes invalid or is unverifiable; advisory work proceeds; no substitution; negotiated compatibility.
12. **Pinned capability meaning.** A reconnecting provider with a changed evaluator neither switches versions, rewrites results, nor reports pass after loss.
13. **Reference participants only**, with coverage limits recorded and same-implementation compositions never described as mixed-implementation proof.
14. **Independent evidence** for what the independent implementation claims, with divergences resolved.
15. **CI** green with manifests, transcripts and mutant summaries, and outcomes kept distinct.

## Status

- **2026-09-15, step 1:**
  - drafts, examples and proposed decisions M5-Q1 to M5-Q9 (`69bc335`);
  - owner decisions incorporated into KNOWLEDGE, VERIFICATION, CONTEXT §14, EXECUTION §13.3, CORE §12, the examples and the matrix (`3edee0f`).
- **2026-09-15, step 2, Knowledge reference provider:**
  - **Contract details** settled while implementing (KNOWLEDGE):
    - the order of the decision, resolve, open and evaluate checks;
    - record defaults for absent members;
    - history `position` as `{ epoch, sequence }`;
    - `authority_epoch` required at step 2.
  - **Dependency cycles.** The `cycle` finding is dropped, because digest-bound dependencies cannot form a cycle. A dependency naming its own revision is `unchecked`.
  - **Owner follow-up on dependency identity:**
    - dependencies resolve only as exact local provider, claim, revision and digest references;
    - `unchecked` carries `reason` (`self_reference`, `remote_dependency`, `unresolved`, `not_evaluated`, `not_established`);
    - references naming another provider are `not_found` in commands;
    - KNOWLEDGE §8 states the validation assumptions.

    The review found that the reference had matched evaluations by claim ID and revision only. It now matches the exact reference everywhere: evaluations, decisions, conflicts and inspect. Fixture: `knowledge.dependencies-resolve-only-exact-references`, with 4 mutants.
  - **Schemas:** `schemas/knowledge/1`, 11 operations. The Core error codes `claims_not_comparable` and `contract_unavailable` are added to the error-data schema and retry classes.
  - **Reference:** `conformance/reference/src/knowledge.rs`. Claims, decisions, evaluations, conflicts and bindings are Core subjects.
  - **Launch control:** `knowledge` (evaluator; adversarial `serve_altered_claims`), declared as `knowledge.store`.
  - **Runner:** new pattern `$canonical_sha256`, so a fixture recomputes a record digest as a reader would.
  - **Fixtures:** 8 in `conformance/fixtures/knowledge`:
    - revisions and base checks;
    - structural conflicts and drift;
    - support classes;
    - independent facets;
    - authority;
    - conflict resolution;
    - applicability precedence;
    - history.
  - **Mutants:** 22 new, each killed by the fixture declaring it.
- **2026-09-15, step 3, Verification reference provider:**
  - **Contract details** settled while implementing (VERIFICATION):
    - evaluator predicates are named `verification.evaluator.<id>.v<version>`, because Core predicate names are dotted and each segment starts with a letter;
    - a caller needs `evidence.read` on the contract's artifact subject;
    - properties are empty when the receipt's bytes cannot be read, as when the contract cannot.
  - **Schemas:** `schemas/verification/1`, 5 operations.
  - **Reference:** `conformance/reference/src/verification.rs`.
    - Contracts are read from the provider's own evidence store.
    - Every receipt is sealed there as `receipt.<id>`, with the subject-to-artifact mapping recorded in the same transaction.
    - Jobs pin their evaluator version and record property results durably.
    - A lost evaluator ends a job with unfinished properties `indeterminate`.
    - Assessment follows VERIFICATION §7.
    - Evidence gained a general `publish_artifact`.
  - **Launch control:** `verifier` (evaluators, scripts), declared as `verifier.script`. Its evaluators are advertised as capability predicates only when configured.
  - **Fixtures:** 5 in `conformance/fixtures/verification`:
    - complete receipts;
    - jobs and pinned evaluators;
    - assessment;
    - reconnect with a changed evaluator (SCN-7, with a knowledge evaluator across the restart);
    - scoped observations and no authority (VER-2 and VER-5, one provider implementing Execution, Knowledge and Verification).
  - **Mutants:** 17 new, each killed.
- **2026-09-15, step 4, claims in packets and claim revalidation:**
  - **Context** (`context.claims`, CONTEXT §14):
    - claim snapshots are read at preparation, from the provider's own knowledge store or a `knowledge_provider` peer, with the record digest recomputed;
    - the `claim_included` check follows the reliance table;
    - an unaccepted `binding` label is published as `hypothesis`;
    - only `invalid_for_target` makes a section stale;
    - an unreadable or altered claim is never carried;
    - packet format `/2`;
    - read-time `claim_changes`, claim entries in `invalidated_items`, and `unverified_items`;
    - sessions without the feature see M4 facts.
  - **Execution** (`execution.claim_revalidation`, EXECUTION §13.3):
    - the executor negotiates `context.claims` at the context provider;
    - `packet.facts` reports `mismatch` (`claim invalidated: …`) or `unavailable` for required items through the §13.1 machinery, so advisory work proceeds and nothing is substituted.
  - **Runner:** `start` configurations may use `$var`, as `start_participant`'s already could. Scripts can then name digests captured before a restart.
  - **Schemas:**
    - the context check `claim_included`;
    - the claim snapshot, `claim_changes`, `unverified_items` and claim invalidations in `context.packet.inspect`;
    - launch configuration `context.knowledge_provider`.
  - **Fixtures:**
    - `context.claims-in-packets-are-negotiated-snapshots`, a single provider covering KNW-9, KNW-10 and CMP-9, including the adversarial altered claim;
    - `composition.claims-in-packets-block-only-the-required-boundary`: separate knowledge, context and executor participants for SCN-16, repeated 5 of 5 as intended.
  - **Mutants:** 9 new, each killed.
  - **Coverage limit:** the composition runs one implementation for every participant; it is not mixed-implementation proof.
- **2026-09-15, step 5, independent spec-only pass and resolutions:**
  - **Helper.** The eleventh spec-only helper extended `conformance/independent/python-core` to `knowledge/1`, `verification/1` and single-provider `context.claims`, from documents alone (base `950f3e0`, implementation committed at `97b5471` before any M5 fixture was read). It ended at 243 pass, 24 skipped, 4 unsupported. Its record is DIVERGENCES §H.M5.
  - **Resolutions** against the written contracts: [M5-DIVERGENCES](M5-DIVERGENCES.md).
    - Six document defects are corrected.
    - Seven reference defects are fixed (overall ignoring checks, an absent evaluator reported `unsupported`, binding under a grant, requested environment reported as observed, receipt event order, receipt ID collision, `receipt.inspect` of unreadable bytes).
    - Twenty-one silent points are written down.
  - **Fixtures.** Seven fixtures moved to version 2 with distinguishing steps.
  - **Mutants.** Ten new, each killed.
  - **Runner:**
    - new pattern `$base64_json`;
    - `check-fixtures` refuses an expectation object that mixes a `$` directive with members, after two such M5 patterns were found to check only their digest.
  - **Twelfth pass (realignment, base `6cec5f6`).** The helper realigned the independent implementation from the resolved documents and passed every fixture without reading one (245 pass, 24 skipped, 4 unsupported). Its open points are resolved in [M5-DIVERGENCES §D](M5-DIVERGENCES.md#d-twelfth-pass-realignment-hm5b):
    - **Contradiction fixed.** The first resolution had put the Core preconditions before the authority epoch and the evaluator capability. KNOWLEDGE and VERIFICATION now follow CORE §10 and §17.
    - **Readings written down.** Dependency reason order, historical claim sections for every reliance, scripted `unmet` never overriding a satisfied item, collision `current`, a job that loses its evaluator before running, reason order, time without the contract, and closed contracts.
    - **Evidence.** Six fixtures gained distinguishing steps and ten new mutants, each killed. Five reference defects are fixed.

## Acceptance criteria: evidence

| # | Criterion | Evidence |
|---|---|---|
| 1 | Separate records | Distinct subject kinds and identities for claims, decisions, evaluations, conflicts, bindings, jobs and receipts (schemas `knowledge/1`, `verification/1`). `knowledge.reliance-applicability-health-and-availability-are-independent` and `verification.results-are-scoped-and-grant-no-authority` show that no operation records another kind implicitly. |
| 2 | Revisions and history | `knowledge.claim-revisions-are-immutable-with-base-checks` (recomputed digests via `$canonical_sha256`, base checks); `knowledge.history-keeps-every-record-with-links` (positions, supersession links, unknown validity). Mutants: `claim-revision-overwritten`, `revise-base-unchecked`, `history-drops-superseded`, `validity-filled-from-recorded`. |
| 3 | Independent facets | `knowledge.reliance-applicability-health-and-availability-are-independent`. Mutants: `single-status-field`, `normative-implies-binding`. |
| 4 | Declared ancestry | `knowledge.support-classes-follow-declared-ancestry`, including `{A,B}`, `{B,C}`, `{A,C}`, wrappers and equal digests. Mutants: `support-entries-as-origins`, `equal-digest-roots-disjoint`, `overlap-as-multiple`. |
| 5 | Structural conflicts | `knowledge.conflicts-are-structural-and-drift-is-not-contradiction`; `knowledge.conflict-resolution-needs-the-bound-authority`. Mutants: `drift-as-conflict`, `conflict-on-identity-only`, `potential-reported-demonstrated`, `resolve-without-authority`. |
| 6 | Authority | `knowledge.only-the-bound-authority-decides`, covering self-adoption, derivation labels, grants, epochs and transfer. Mutants: `decide-by-grant-right`, `derivation-label-authorizes`, `self-adoption-unrecorded`, `transfer-resets-reliance`, `decision-epoch-optional`, `decision-supersession-unchecked`, `bind-by-grant`. |
| 7 | Applicability | `knowledge.applicability-follows-precedence`; `knowledge.applicability-checks-snapshots-and-environments`; in packets, `context.claims-in-packets-are-negotiated-snapshots`. Mutants: `unknown-outranks-mismatch`, `incomplete-coverage-applicable`, `dirty-snapshot-as-tree`, `unknown-applicability-marked-stale`. |
| 8 | Complete receipts | `verification.receipts-list-every-role-and-property`; `verification.evaluate-contract-returns-a-job-and-pins-its-evaluator`. Mutants: `receipt-listing-incomplete-accepted`, `results-in-outcome`, `evaluator-version-substituted`. |
| 9 | No pass by absence or age | `verification.assessment-never-passes-missing-unavailable-or-outdated`. Mutants: `not-evaluated-satisfied`, `contract-digest-only`, `subject-digest-ignored`, `time-basis-ignored`, `before-observation-ignored`, `stale-ignored`, `environment-unverified-satisfied`, `assess-roles-incomplete-accepted`, `receipt-bytes-unchecked`. |
| 10 | Scoped observations | `verification.results-are-scoped-and-grant-no-authority`. Mutants: `failure-marks-execution`, `receipt-authorizes-decision`. |
| 11 | Packets carrying claims | `context.claims-in-packets-are-negotiated-snapshots` (single provider, including the altered claim); `composition.claims-in-packets-block-only-the-required-boundary` (separate participants). Mutants: `label-promotes-claim`, `claim-latest-substituted`, `claim-digest-unchecked`, `claims-without-negotiation`, `claim-invalidation-unreported`, `claim-revalidation-ignored`, `advisory-blocked-by-claims`, `unavailable-knowledge-valid`. |
| 12 | Pinned capability meaning | `verification.reconnect-with-changed-evaluator-keeps-pinned-meaning` (SCN-7, with a knowledge evaluator across the restart). Mutants: `lost-evaluator-reports-pass`, `lost-evaluator-switches-version`, `recorded-results-rewritten`. |
| 13 | Reference participants only | Scripted evaluator, knowledge controls and the M4 scripted participants; no CBR engine, PIO adapter or Coordination. Compositions run one implementation for every participant. |
| 14 | Independent evidence | Step 5, spec-only pass: see [Status](#status) once resolved. |
| 15 | CI | Linked from [PR #6 checks](https://github.com/Combraton/protocol/pull/6/checks). |

## M4 coverage carried forward

These gaps were accepted with M4. Their dispositions are proposals. None is automatically an M5 blocker, and the list stays open for the owner to reorder.

| Gap | Impact | Planned disposition |
|---|---|---|
| No mixed-implementation composition: every participant in a composition runs the descriptor under test | Cross-implementation interoperability of Context, Evidence and Execution is unproven; only single-provider behavior is independently checked | **M6**, with the minimal third-party executor and evidence publisher. M5 compositions carry the same limit and say so. |
| The independent provider has no Unix-socket binding or backpressure, so it skips compositions (23 skipped) | Composition and socket semantics are checked only on the reference | **M6** candidate, together with the previous row. The M5 independent pass stays on stdio. |
| No fairness among released executions | Released work may wait behind newly queued work; there is no starvation bound | **Accepted 0.1 limit.** Revisit when an executor consumer needs a guarantee. |
| One adversarial store control (altered bytes) | Lying providers beyond digest checks (altered facts, forged references) are untested | **M5 adds one case** at the new trust boundary: an altered claim record under its original digest is never carried (`context.claims-in-packets-are-negotiated-snapshots`, knowledge control `serve_altered_claims`). Wider adversarial providers go to the backlog. |
| Evidence edge cases: `evidence.availability.changed`; fetch shrinking to small receive limits; a sealed but unavailable manifest child; `released_holds` filtering; rarer credential-locator forms | Implemented in the reference but unguarded, so independent implementations may diverge | **Backlog**, for an M6 hardening pass if time allows. Not blockers. |
| Context edge cases: script steps `end` and `unmet`; `omit` naming an item; `dirty_snapshot` and `environment_digest` conditions; cancel with no remaining subscriber; job-event visibility; shared jobs with different fallbacks; corrections on advisory items | Low to medium | **M5 covers** `environment_digest` and `dirty_snapshot` for claim applicability (`knowledge.applicability-checks-snapshots-and-environments`). Context's own use of them, and the other listed cases, go to the **backlog**. |
| Revalidation edge cases: advisory checks at dispatch; both deadlines, or inactivity, due when released work ends | Low | **Backlog.** |
| `observed` strings of `packet.facts` and `packet.current` checked on the reference only | Wording drift across implementations is undetected | With mixed composition, **M6**. |
| Carried from M3: all Execution evidence is scripted; barrier- and signal-synchronized fixtures limit other participants | Real adapter behavior is not established | **Accepted 0.1 scope limit**; real adapters are PIO's gate. |
| CMP-5: machine-readable feature-dependency advertising | Dependencies are published in profile documents only | **M6 decision**, as already scheduled. Knowledge and Verification publish their Core dependencies the same way. |
| Carried from M2: root is the only other OS user tested; capability evidence-source and cursor-past-head checks are reference-only | Narrow | **Accepted limits.** |
